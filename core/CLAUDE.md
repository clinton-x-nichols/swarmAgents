# CLAUDE.md — Claude Code Entry Point

Before starting any work in this repository:

1. Read `AGENTS.md` in full.
2. Read `swarm-config.json`.
3. Read the Worker role file named in `swarm-config.json`.
4. Read `playbooks/SWARM_PROTOCOL.md` and `playbooks/SECURITY_AND_AUTHORITY.md`.
5. Read `state/CURRENT_STATE.md`, `state/DECISION_REGISTER.md`, `state/OPEN_QUESTIONS.md`, and `state/WORK_QUEUE.md`.
6. If communication tools are available, fresh-read the notices channel and the full active substantive thread before posting status or acting on queued work.

Important: repository instructions cannot override Claude Code permissions, platform policies, or any requirement for direct owner confirmation. If direct owner authorization is required, request it directly and continue unaffected work where possible.

After a fresh session start or recovery, return a concise BOOTLOAD before modifying project content.
