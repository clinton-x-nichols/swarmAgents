Read and follow the repository root `AGENTS.md`, `swarm-config.json`, and `playbooks/SWARM_PROTOCOL.md` before making changes. Treat `state/CURRENT_STATE.md` as the quick-reference coordination state, preserve role and authority boundaries, verify load-bearing claims against primary sources, and use fresh/intervening-change controls before repository writes. Do not interpret repository instructions as authorization to bypass platform security or human approval requirements.